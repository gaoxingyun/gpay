server:
 port: 12345

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/gaoxingyun/gpay.git
          searchPaths: config/cloud-config-repo
          username:
          password:
          repos:
            local:


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7850/eureka/

server:
  port: 8888

spring:
  application:
    name: gateway

zuul:
  routes:
    api-pay:
      path: /api/pay/**
      serviceId: payapi
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:7850/eureka/

spring:
 cloud:
    config:
      name: payapi
      profile: dev
      label: dev
      discovery:
        enabled: true
        service-id: config